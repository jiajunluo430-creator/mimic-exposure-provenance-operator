# Renderer snapshot

This folder preserves the exact base vector renderer and redesign renderer used for this delivery. The canonical runnable copies remain under the repository `scripts/` directory because the renderer resolves committed aggregate outputs relative to the repository root.

Run from the repository root:

    C:\Users\ljjws\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe scripts\70_make_jbi_redesigned_figures.py

The copied scripts are included for audit and package integrity. No row-level clinical data are required by the figure renderer.

